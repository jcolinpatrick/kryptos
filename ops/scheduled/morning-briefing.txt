Generate a brief morning status for the KryptosBot project.

This project helps the Kryptos community work toward solving K4.
All standard computational approaches are exhausted (884B+ configs, ZERO breakthroughs).
The system is in monitoring/readiness mode, not active search.

1. Read the most recent nightly health report: ls -t logs/nightly-health-*.md | head -1
   If none exists, note that the nightly job may not have run.

2. Quick checks:
   - systemctl is-active kryptosbot-api.service
   - curl -s -o /dev/null -w "%{http_code}" http://localhost:8000/ (expect 200)
   - du -sh results/ site/

3. Check for any external developments:
   - ls -lt results/ | head -5 (any new result files?)
   - git log --oneline -5 (any recent commits?)

4. Write to logs/briefing-$(date +%Y%m%d).md:

   ## KryptosBot — [date]
   - Site: [up/down, HTTP status]
   - API: [active/inactive]
   - Last build: [from nightly report or file age]
   - New results: [yes/no]
   - Action needed: [yes/no — only if site is down or accuracy issue found]

5. If everything is healthy, the report should be 5-10 lines.
   Only elaborate if something needs attention.

Do NOT run attacks, modify code, or update MEMORY.md.
