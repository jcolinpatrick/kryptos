"""Constraint checking for K4 cryptanalysis.

Provides crib matching, Bean constraint verification, and general consistency checks.
All constraints can operate as fast prefilters AND as post-checks,
and emit diagnostic information about WHY a candidate fails.
"""
